DKExpandableSelectionTree
=========================

Control for Android that allows user to select multiple items from a tree of objects. It is a subclass of ExpandableListActivity with an example of implementation.
